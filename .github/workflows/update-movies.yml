name: به‌روزرسانی خودکار فیلم‌ها

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      
      - name: نصب Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      
      - name: نصب وابستگی‌ها
        run: |
          cd src
          pip install -r requirements.txt
          pip install --upgrade requests beautifulsoup4 imdb-core
      
      - name: اجرای اسکریپت دریافت اطلاعات
        env:
          IMDB_COOKIES: ${{ secrets.IMDB_COOKIES }}
          OMDB_API_KEY: ${{ secrets.OMDB_API_KEY }}
        run: |
          cd src
          python fetch_imdb.py
      
      - name: ارسال تغییرات
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/movies.json
          git diff --staged --quiet || git commit -m "به‌روزرسانی خودکار فیلم‌ها [skip ci]"
          git push
